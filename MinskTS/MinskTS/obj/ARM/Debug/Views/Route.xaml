<Page
    x:Class="MinskTS.Views.Route"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MinskTS.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <AutoSuggestBox x:ConnectionId='1' x:Name="suggestRoute"  PlaceholderText="Введите фразу для поиска" QueryIcon="Find"                                       VerticalAlignment="Top" HorizontalContentAlignment="Stretch"/>

        <ListView x:ConnectionId='2' x:Name="scheduleRoute" Margin="0,32,0,0">

            <ListView.ItemTemplate>
                <DataTemplate                        >
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"  />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding Number}" FontSize="26" />
                        <TextBlock Grid.Column="1" Text="{Binding RouteName}" FontSize="26" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>

        </ListView>
    </Grid>
</Page>

